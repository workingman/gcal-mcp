{
  "name": "calendar-mcp",
  "version": "0.1.0",
  "description": "Google Calendar MCP Server for Claude - Cloudflare Workers",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir=dist",
    "deploy": "wrangler deploy",
    "test": "node --test --experimental-test-coverage tests/**/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "keywords": [
    "mcp",
    "google-calendar",
    "cloudflare-workers",
    "claude"
  ],
  "author": "Sense and Motion",
  "license": "MIT",
  "dependencies": {
    "@cloudflare/workers-oauth-provider": "^0.2.0",
    "@modelcontextprotocol/sdk": "^1.0.4",
    "agents": "^0.1.0",
    "hono": "^4.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250115.0",
    "@types/node": "^22.10.5",
    "typescript": "^5.9.0",
    "wrangler": "^3.100.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
